与方法调用不同，继承打破了封装性  子类依赖父类中的特定功能实现，父类的实现可能会根据版本不同而有所变化  子类可能会遭到破坏

复合 composition 将现有的类的实例引进，当作新类的一个组件


只有当子类是真正的父类的子类型时，才适合用继承 换句话说对于两个类A和B 只要当两者之间确实存在 is-a的关系时候，类B才能应该扩展A
如果打算让类B扩展A，就应该问问自己，每个B确实也是A吗？如果答案不是肯定，那么B不应该扩展A，
 如果是否定的，B应该包含A的一个私有实例，并且暴露一个较小的较简单的API,A本质上不是B的一部分，只是他的实现细节而已。