数组与泛型有两个不同点
  1、数组是协变的（covariant） 如果Sub为Supper的子类型，那么数组Sub[]就是Supper的子类型
     泛型是不可变的（invariant） 任意两个不同类型Type1和Type2, List<Type1>既不是List<Type2>的子类型，也不是List<Type2>的超类型

  2、数组是具体化的，因此数组会在运行的时候才会知道并检查他们元素的约束
     泛型通过擦除来实现，只有在编译的时候强化他们的类型信息，并在运行的时候丢弃或者擦擦他们的元素类型信息；
     擦除即使泛型可以和没有使用泛型的代码随意进行户用


  由此可知:泛型数组使非法的，因为他们不是类型安全，要是他们合法，编译器在其他的正确程序中发生的转换就会运行失败，并出现一个ClassCastException
